import { default as swagger } from './swagger';
import { default as cors } from './cors';
import sequelize from './sequelize';
import { default as morgan } from './morgan';

export { swagger, cors, sequelize, morgan };
